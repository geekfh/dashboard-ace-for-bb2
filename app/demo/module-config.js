/**
 * ui config
 * @example
 * var gridWidth = Opf.Config._("ui", "grid.form.width");
 * var othersIsOms = Opf.Config._("others", "isOms");
 */
define({
    ui: {
        'grid.form.width': 360,
        'grid.form.height': 420
    },

    others: {
        isOms:true,
        isOps:false
    }
});
